export const metadata = {
  title: 'Multi-AI Comparator',
  description: '4 IA répondent en parallèle, un juge désigne la meilleure réponse.',
};

export default function MultiAiLayout({ children }) {
  return <>{children}</>;
}
